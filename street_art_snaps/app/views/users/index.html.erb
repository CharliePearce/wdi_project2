<p id="notice"><%= notice %></p>

<h1>Street Art People.</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th>Avatar</th>
      <th>Name</th>
      <th>Location</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td> 
        <td><%= image_tag user.user_image.thumb.url %></td> 
        <td><%= user.full_name %></td>
        <td><%= user.location.name if user.location %></td>
        <td><%= link_to 'View profile', user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

